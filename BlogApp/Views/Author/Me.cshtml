@model BlogApp.Models.Author
@{
    ViewBag.Current = "Me";
}
<div style="display: flex; justify-content: center; align-items: flex-start; min-height: 500px; margin-top: 60px;">
    @Html.Partial("_AuthorSidebar")
    <div style="flex:1; min-width:380px; background: #fff; border-radius: 16px; box-shadow: 0 2px 16px rgba(50,80,120,0.11); padding: 46px 64px 38px 64px;">
        <h2 style="font-weight:700; margin-bottom:14px;">你好，@Model.User.UserName</h2>
        <div style="font-size:1.2rem;color:#888;margin-bottom:30px;">
            欢迎来到作家工作台
        </div>
        <!-- 可继续添加你的工作台统计、公告等内容 -->
    </div>
</div>
